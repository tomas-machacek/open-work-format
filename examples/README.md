# Examples

This directory is reserved for non-normative OWF workspace examples.

Concrete examples are intentionally deferred until the Markdown/YAML
representation profile defines how Core concepts are serialized. Examples will
illustrate the specification but will not define OWF semantics.
